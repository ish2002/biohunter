import React from "react";
import { LoginForm } from "../accountBox/loginForm.jsx";

export default class LogIn extends React.Component {
    render() {
        return (
            <>
                <LoginForm/>
            </>
        );
    }
}